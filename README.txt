* Run "composer install" to get all dependencies.
then :
* Add [full-wrapper-path]/bin/combawa-wrapper to you PATH
OR
* Create a symlink to you bin directory
  exemple : "ln -s [full-wrapper-path]/bin/combawa-wrapper /usr/bin/combawa"
